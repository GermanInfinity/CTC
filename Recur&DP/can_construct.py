# canConstruct: is it possible to generate a target word, with elements in array

# make base case 
def can_construct(target, wordBank):
	if target == "": return True
	#for word in wordBank:
    # find prefix
    # make recursive call on suffix 
    # if can construct return true

    # else false

# memoize to improve performance 
